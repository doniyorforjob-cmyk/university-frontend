import apiClient from '../client';
import { MediaArticle } from '../../types/media.types';

export const getMediaArticles = async (): Promise<MediaArticle[]> => {
  // TODO: Haqiqiy API so'rovi shu yerda yoziladi
  console.warn("getMediaArticles: http versiyasi hali yozilmagan.");
  return [];
};