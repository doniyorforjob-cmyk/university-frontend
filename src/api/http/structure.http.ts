import apiClient from '../client';
import { Department } from '../../types/structure.types';

export const fetchStructureData = async (): Promise<Department[]> => {
  // TODO: Haqiqiy API so'rovi shu yerda yoziladi
  console.warn("fetchStructureData: http versiyasi hali yozilmagan.");
  return [];
};